<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
    
    <PropertyGroup>
        <Version>1.1.1-dev1</Version>
        <ReleaseNotes>Add IDocumentService. CommandLineService use IDocumentService.</ReleaseNotes>
    </PropertyGroup>
</Project>