<Project>

    <PropertyGroup>
        <Version>2.1.5-dev001</Version>
        <NotRefenceApi>true</NotRefenceApi>
    </PropertyGroup>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
    
</Project>