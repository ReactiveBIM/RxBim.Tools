<Project>
    
    <PropertyGroup>
        <ApiVersion>1.0.0-dev02</ApiVersion>
        <ReleaseNotes>Add IDocumentService. CommandLineService use IDocumentService.</ReleaseNotes>
    </PropertyGroup>

    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))"/>
    <Import Project=".\RxBim.Build.Props" Condition="Exists('.\RxBim.Build.Props')"/>
</Project>