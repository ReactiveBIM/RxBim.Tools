<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

    <PropertyGroup>
        <Version>2.0.2-dev01</Version>
        <PackageReleaseNotes>Add ITableDeserializer, ObjectCellContent</PackageReleaseNotes>
    </PropertyGroup>
</Project>